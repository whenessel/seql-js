# Исправление экранирования ведущего дефиса в Tailwind классах

## ✅ Проблема решена

Классы Tailwind CSS, начинающиеся с дефиса (например, `-bottom-6`, `-left-6`, `-mt-4`), теперь правильно экранируются в CSS селекторах.

## 🔧 Что было исправлено

### Файл: `src/resolver/css-generator.ts` (строки 1241-1254)

**Метод `escapeCSS()`** обновлен для правильного экранирования ведущего дефиса согласно спецификации CSS.

### До исправления:
```typescript
private escapeCSS(str: string): string {
  return str.replace(/([!"#$%&'()*+,./:;<=>?@[\\\]^`{|}~])/g, '\\$1');
}
```

### После исправления:
```typescript
private escapeCSS(str: string): string {
  let escaped = str;
  
  // Если строка начинается с дефиса, экранируем его
  if (escaped.startsWith('-')) {
    escaped = '\\-' + escaped.slice(1);
  }
  
  // Экранируем другие специальные символы
  escaped = escaped.replace(/([!"#$%&'()*+,./:;<=>?@[\\\]^`{|}~])/g, '\\$1');
  
  return escaped;
}
```

## 📊 Результаты

### Проблемный DOM из задачи:
```html
<section id="welcome">
  <div class="container">
    <div class="absolute -bottom-6 -left-6">
      <img src="..." alt="Luxury apartment interior">
    </div>
  </div>
</section>
```

### Было (неправильно):
```
SEQL: v1.0: section[id="welcome"] :: div.container#1 > div.-bottom-6.-left-6#2
CSS:  section#welcome div.-bottom-6  ❌ Invalid selector!
```

### Стало (правильно):
```
SEQL: v1.0: section[id="welcome"] :: div.container#1 > div.-bottom-6.-left-6#2
CSS:  section#welcome div.\-bottom-6  ✅ Valid selector!
```

## 🧪 Тестирование

### Добавлено 6 новых тестов:

**Unit тесты** (`tests/unit/css-generator.test.ts`):
1. ✅ Экранирование `-bottom-6`, `-left-6`
2. ✅ Экранирование `-mt-4`, `-mx-2`, `-z-10`
3. ✅ Только ведущий дефис экранируется (не внутренние)

**Integration тесты** (`tests/integration/leading-dash-fix.test.ts`):
1. ✅ Резолв элемента с отрицательными margin классами
2. ✅ Резолв вложенного img с родителем, содержащим ведущий дефис
3. ✅ Обработка SEQL selector формата с ведущим дефисом

### Все тесты прошли:
```
✓ 219 тестов пройдено
✓ 15 тестовых файлов
✓ Сборка проекта успешна
```

## 🎯 Какие Tailwind классы теперь работают

Все Tailwind классы с ведущим дефисом теперь корректно работают:

- **Отрицательные отступы**: `-m-1`, `-m-2`, `-mt-4`, `-mx-2`, `-my-8`
- **Отрицательное позиционирование**: `-top-4`, `-bottom-6`, `-left-6`, `-right-8`
- **Отрицательный inset**: `-inset-0`, `-inset-x-4`, `-inset-y-2`
- **Отрицательный z-index**: `-z-10`, `-z-20`
- **Отрицательный space**: `-space-x-2`, `-space-y-4`

## 📝 Изменённые файлы

1. `src/resolver/css-generator.ts` - Обновлен метод `escapeCSS()`
2. `tests/unit/css-generator.test.ts` - Добавлено 3 unit теста
3. `tests/integration/leading-dash-fix.test.ts` - Добавлено 3 integration теста (новый файл)
4. `LEADING_DASH_FIX_DEMO.md` - Документация на английском
5. `ИСПРАВЛЕНИЕ_ВЕДУЩЕГО_ДЕФИСА.md` - Документация на русском

## 🚀 Как проверить

1. **Запустить тесты:**
   ```bash
   yarn test tests/unit/css-generator.test.ts
   yarn test tests/integration/leading-dash-fix.test.ts
   ```

2. **Запустить все тесты:**
   ```bash
   yarn test
   ```

3. **Собрать проект:**
   ```bash
   yarn build
   ```

## ✨ Критерии успеха (из задачи)

- [x] SEQL Selector `v1.0: section[id="welcome"] :: div.container#1 > div.-bottom-6.-left-6#2` корректно резолвится
- [x] CSS селектор содержит `div.\-bottom-6` вместо `div.-bottom-6`
- [x] `querySelectorAll()` не выбрасывает ошибку
- [x] Элемент с классами `-bottom-6` и `-left-6` успешно находится в DOM
- [x] Все тесты проходят (219/219)
- [x] Сборка проекта успешна

## 📚 Соответствие стандартам

Исправление соответствует спецификации CSS:
- [CSS Syntax Module Level 3 - Identifiers](https://www.w3.org/TR/css-syntax-3/#ident-token-diagram)
- Ведущий дефис требует экранирования: `.\-class` вместо `.-class`
- Внутренние дефисы валидны: `.my-class` правильный селектор

## 🎉 Заключение

Проблема полностью решена! Все Tailwind классы с ведущим дефисом теперь корректно обрабатываются библиотекой SEQL-JS.
