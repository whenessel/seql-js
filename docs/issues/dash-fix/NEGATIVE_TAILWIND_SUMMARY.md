# 📋 КРАТКАЯ СВОДКА: Проблема с отрицательными Tailwind классами

## 🔴 КОРНЕВАЯ ПРИЧИНА

Фильтр классов `src/utils/class-classifier.ts` **НЕ** распознает Tailwind утилиты с ведущим дефисом как нестабильные классы:
- `-bottom-6`, `-left-6`, `-mt-4`, `-z-10` и т.д.

## 💥 ПОСЛЕДСТВИЯ

1. Эти классы **попадают в EID** как стабильные семантические классы
2. Генератор создает SEQL: `div.-bottom-6.-left-6#2`
3. Парсер **не может разобрать** классы с ведущим дефисом
4. **Ошибка**: `Invalid node: unexpected content ".-bottom-6.-left-6#2"`

## ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ

### ❌ НЕПРАВИЛЬНО (мое первое предложение)
Исправить парсер, чтобы он парсил классы с дефисом

### ✅ ПРАВИЛЬНО (по спецификации)
**Исправить фильтр классов**, чтобы утилиты с дефисом вообще НЕ попадали в SEQL селектор

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

1. Добавить паттерны в `src/utils/class-classifier.ts`:
   ```typescript
   /^-[mp][trblxy]?-\d+$/,           // -m-4, -mt-2
   /^-(top|right|bottom|left|inset)-\d+$/,  // -bottom-6
   /^-z-\d+$/,                        // -z-10
   /^-space-[xy]-\d+$/,               // -space-x-2
   // и другие...
   ```

2. Добавить тесты в `tests/unit/class-classifier.test.ts`

3. Создать integration тест `tests/integration/negative-tailwind-filter.test.ts`

## 📄 ПОЛНАЯ ИНСТРУКЦИЯ

См. файл: **`NEGATIVE_TAILWIND_FIX.md`**

Этот файл содержит:
- Детальную диагностику
- Пошаговые инструкции с кодом
- Критерии успеха
- Способы проверки

---

## 💡 КЛЮЧЕВОЙ ИНСАЙТ

**Отрицательные Tailwind классы** (`-mt-4`, `-bottom-6`) — это **такие же утилиты**, как и положительные (`mt-4`, `bottom-6`). Они НЕ должны использоваться в SEQL селекторах по спецификации, так как нестабильны и меняются при рефакторинге дизайна.

SEQL селекторы должны опираться только на **семантические, стабильные** классы типа:
- `nav-item`, `button-primary`, `card-header`, `hero-banner`
